async function interfaces(file){
  const initialState = {
    time: new Date().getTime()
  };
  
  switch(file){
    case '/Login':
      break;
  }
  
  return initialState;
}

module.exports = interfaces;
